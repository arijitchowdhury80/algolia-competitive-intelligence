# Data

This folder stores benchmark outputs and future data exports.

The active source registry currently lives at:

`packages/ci-core/references/sources.yaml`

Do not duplicate or fork source strategy until the config loader supports an external `CI_SOURCES_FILE` path.

