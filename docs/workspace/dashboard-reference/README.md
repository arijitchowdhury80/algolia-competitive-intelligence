# Dashboard reference copy

`index.html` in this directory is a preserved copy of the live `ci.chowmes.com`
page (source: `/tmp/dash-ui/public/index.html`, 15,983 bytes, captured
2026-07-08). Arijit designed this page personally. It is the design contract
for `src/cios/dashboard/html_renderer.py` — structure, CSS classes, inline
styles, and section order all come from this file. The `/tmp` copy is
ephemeral; this one is the durable reference.
